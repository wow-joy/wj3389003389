---
name: Snack(全局提示)
route: /snack
menu: Components(基础组件)
---
import { Playground, Props } from 'docz'
import { Snack } from '@wowjoy/core'


# Snack
<Playground>
  <button onClick={() => Snack.success('success')}>success</button>
  <button onClick={() => Snack.error('error')}>error</button>
  <button onClick={() => Snack.info('info')}>info</button>
  <button onClick={() => Snack.warning('warning')}>warning</button>
  <button onClick={() => Snack.destroy()}>destroy</button>
</Playground>

## config
<Playground>
  <b>Snack.config 全局设置，需要提前设置</b>
  <div>top: Number</div><div>duration: Number</div>
  <i>{Snack.config({top: 500, duration: 3000})}</i>
</Playground>

